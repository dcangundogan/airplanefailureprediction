# Archive

This folder keeps files that are useful as references but are not clean runnable scripts.

- `code_exports/BEGUM_CODE.py` is a Python interactive-console export.
- `code_exports/bigru-tcn-xgboost.py` contains copied prompt/console continuation markers.

Runnable Python files are in `../scripts/`.
