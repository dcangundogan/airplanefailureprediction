# Notebooks

This folder contains the Jupyter and Colab experiments moved from the project root.

Many notebooks were authored in Google Colab and still use paths such as `/content/drive/MyDrive/data`. For local execution, point the notebook path cell at `../data` if the kernel starts inside this folder, or at `data` if the kernel starts from the project root.
